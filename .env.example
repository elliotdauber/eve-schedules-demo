# AI Gateway — required for Eve agent and scheduled prompts
AI_GATEWAY_API_KEY=

# Vercel Blob — linked store id (OIDC auth is automatic on Vercel; run `vercel env pull` locally)
# BLOB_STORE_ID=

# Optional: override model for scheduled prompts
# SCHEDULED_PROMPT_MODEL=openai/gpt-4o-mini
